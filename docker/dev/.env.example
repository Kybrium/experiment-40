# --- MySQL ---
MYSQL_DATABASE=exp40
MYSQL_USER=exp40
MYSQL_PASSWORD=exp40
MYSQL_ROOT_PASSWORD=root

# --- Backend (Django) ---
DJANGO_SECRET_KEY=dev-secret
DJANGO_DEBUG=True
DJANGO_MODE=DEV
DJANGO_HOSTS=backend,localhost,127.0.0.1
DJANGO_HOSTS_URLS=http://localhost:3000,http://127.0.0.1:3000

# DB connection for Django
DB_ENGINE=mysql
DB_NAME=${MYSQL_DATABASE}
DB_USER=${MYSQL_USER}
DB_PASSWORD=${MYSQL_PASSWORD}
DB_HOST=db
DB_PORT=3306

# --- Frontend (Next.js) ---
NEXT_PUBLIC_APP_NAME=Experiment 40
NEXT_PUBLIC_MODE=DEV
NEXT_PUBLIC_API_URL=http://localhost:8000